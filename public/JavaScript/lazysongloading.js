
document.addEventListener('songs', function(){
    //get all song via thier class
    const songs = document.querySelectorAll('song')

    //options for intersection observation 
    const potion = {
        root:null, // seting view port to load music
        thredshod: 0.5 // trigger when view port is 50%
    }

    // run this function if the above option is true
    songs.forEach(song=>{
        
    })
})